services:
  - type: web
    name: reveste
    env: python
    buildCommand: "pip install -r requirements.txt && prisma generate --schema=prisma/schema.prisma"
    startCommand: "gunicorn app:app"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: reveste-db
          property: connectionString